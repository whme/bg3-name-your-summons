The native settings panel no longer has a Save button: every change now applies
the moment you make it. Editing a saved name applies when the field loses focus,
and a forgotten name keeps its Undo until the panel is closed.
