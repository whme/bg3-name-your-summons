Opening the native settings panel from the Examine gear and changing the
multi-summon mode no longer lags or crashes the game. The three mode toggles
enforced their radio behaviour with a guard that did not hold across Noesis'
deferred property callbacks, so each change multiplied into an ever-growing
storm of callbacks; they now update only when a value actually changes, so the
cascade cannot start.
