Removed the old ImGui-based naming prompt and settings window, along with the
`!nys_ui` console command that opened them. Naming and configuration now live
entirely in the native Examine panel - the rename field and the settings gear.
