<ls:UIWidget x:Name="Examine"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:ls="clr-namespace:ls;assembly=Code"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:noesis="clr-namespace:NoesisGUIExtensions;assembly=Noesis.GUI.Extensions"
             xmlns:System="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             d:DesignWidth="3840" d:DesignHeight="2160"
             ls:UIWidget.ContextName="Examine" d:DataContext="{d:DesignInstance {x:Type ls:DCExamine}, IsDesignTimeCreatable=True}"
             ls:TooltipExtender.Owner="{Binding CurrentPlayer.UIData.ExamineTarget}">

    <ls:UIWidget.Resources>
    <!-- A boxed Noesis string the client reads back with element:Resource("NYS_CloseWidget")
         to close this panel from Lua: CustomEvent's "CloseWidget" parameter must be a boxed
         Noesis value, which SE cannot mint. Do not remove; NativeRenameUI depends on it. -->
    <System:String x:Key="NYS_CloseWidget">CloseWidget</System:String>
    <ControlTemplate x:Key="CharSheetTabController" TargetType="Control">
        <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <TabControl>
                <TabControl.Resources>
                    <Style TargetType="TabItem" BasedOn="{StaticResource TabItemStyle}">
                        <Setter Property="MinWidth" Value="{Binding ActualWidth, ElementName=SummaryPanelContent, Converter={StaticResource DivideConverter}, ConverterParameter=3}"/>
                    </Style>
                </TabControl.Resources>
                <!-- ====== SUMMARY TAB =======-->
                <TabItem x:Name="SummaryTab" IsSelected="True" >
                    <TabItem.Header>
                        <Border>
                            <Border.ToolTip>
                                <ls:LSTooltip Content="{Binding Source='h890f4cddg2bdcg4bb2gb73eg43fe16b11b08',Converter={StaticResource TranslatedStringConverter}}"/>
                            </Border.ToolTip>
                            <Image Width="60" Height="60" Source="pack://application:,,,/Core;component/Assets/CharacterCreation/ico_class.png" Stretch="Uniform"/>
                            
                        </Border>
                    </TabItem.Header>

                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <ScrollViewer Grid.Row="0" Style="{StaticResource NoForcedScrollViewerStyle}" Padding="{StaticResource ScrollOffset}">

                            <Grid x:Name="SummaryPanelContent">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <StackPanel Grid.Row="0">
                                    <!-- ======== NAME Stack For NPC ======== -->
                                    <Grid x:Name="NamePanelNPC" Visibility="{Binding IsPlayableCharacter, Converter={StaticResource BoolToCollapsedConverter}}">
                                        <StackPanel Grid.Row="0">
                                            <TextBlock x:Name="NPCType" Text="{Binding Stats.Race.Type}" HorizontalAlignment="Center" Style="{StaticResource DetailedListText}" TextAlignment="Center" TextWrapping="Wrap"/>
                                            <TextBlock x:Name="NPCRace" HorizontalAlignment="Center" FontSize="{DynamicResource ScaledSmallishFontSize}" TextAlignment="Center" TextWrapping="Wrap">
                                                <TextBlock.Text>
                                                    <MultiBinding Converter="{StaticResource ParameterizedTranslatedStringConverter}">
                                                        <Binding Source="hd678d4eeg181fg499dg9e81g2146ab24e03a"/>
                                                        <Binding Path="Stats.Level.Value"/>
                                                        <Binding Path="Stats.Race.DisplayName"/>
                                                    </MultiBinding>
                                                </TextBlock.Text>
                                                <TextBlock.ToolTip>
                                                    <ls:LSTooltip IsHitTestVisible="False" Content="{Binding DataContext.Stats.Race.Description, ElementName=NPCRace}" ls:AttachedProperties.InheritedTag="{Binding DataContext.Stats.Race.DisplayName, ElementName=NPCRace}" ContentTemplate="{StaticResource HeaderBodyLocaStringTemplate}"/>
                                                </TextBlock.ToolTip>
                                            </TextBlock>

                                            <Grid x:Name="HealthbarPortrait">
                                                <Grid x:Name="HealthbarPortraiterh" Height="220" Width="152">
                                                    <ContentPresenter x:Name="portrait" ContentTemplate="{StaticResource CharacterPortraitTemplate}" Content="{Binding}"/>
                                                    <Image x:Name="Frame" Source="{StaticResource FrameImg_d}" Stretch="Uniform" IsHitTestVisible="False"/>
                                                </Grid>
                                                <Control Template="{StaticResource ACHolder}" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,36,0"/>

                                            </Grid>
                                            <TextBlock Text="{Binding Name}" HorizontalAlignment="Center" FontSize="{DynamicResource ScaledBigFontSize}" TextAlignment="Center" TextWrapping="Wrap">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding CharacterType}" Value="Summon">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                            <!-- Name Your Summons: the summon's name + settings gear (summons only).
                                                 The name box defaults to read-only/Focusable/IsHitTestVisible all off - the
                                                 native plain-name look, correct for a forbidden summon; NativeRenameUI flips
                                                 those input-checked flags on for a renamable one.
                                                 OpenVirtualKeyboardOnFocus=False: with Steam Input active (Big Picture) the
                                                 default (True) pops Steam's on-screen keyboard on focus. -->
                                            <StackPanel x:Name="NYS_RenameBar" Orientation="Vertical" HorizontalAlignment="Center">
                                                <StackPanel.Style>
                                                    <Style TargetType="StackPanel">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding CharacterType}" Value="Summon">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </StackPanel.Style>
                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                    <ls:LSTextBox x:Name="NYS_NameInput" Text="{Binding Name, Mode=OneWay}" IsReadOnly="True" Focusable="False" IsHitTestVisible="False" OpenVirtualKeyboardOnFocus="False" MinWidth="200" MaxLines="1" TextWrapping="NoWrap" TextAlignment="Center" VerticalAlignment="Center" FontSize="{DynamicResource ScaledBigFontSize}" SelectTextOnFocus="True"/>
                                                    <!-- An LSButton with a Command binding (a Grid has no Click/command). Its
                                                         DataContext is a small NYS_GearVM set from Lua (NativeRenameUI). -->
                                                    <ls:LSButton x:Name="NYS_SettingsButton" Width="40" Height="40" Margin="10,0,0,0" VerticalAlignment="Center"
                                                                 Background="Transparent" Command="{Binding NysGearCommand}">
                                                        <ls:LSButton.Template>
                                                            <ControlTemplate TargetType="{x:Type ls:LSButton}">
                                                                <Grid Background="Transparent">
                                                                    <Image x:Name="NYS_GearIcon" Stretch="Uniform"
                                                                           Source="pack://application:,,,/Core;component/Assets/CharacterAssign/ico_settings_d.png"/>
                                                                </Grid>
                                                                <ControlTemplate.Triggers>
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter TargetName="NYS_GearIcon" Property="Source"
                                                                                Value="pack://application:,,,/Core;component/Assets/CharacterAssign/ico_settings_h.png"/>
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </ls:LSButton.Template>
                                                    </ls:LSButton>
                                                </StackPanel>
                                                <!-- Confirm commits + advances the queue; Skip skips this summon. VMs set from
                                                     Lua; both share NysShow* (#examineQueue > 0), so both hide for single summons
                                                     and on the last of a group. -->
                                                <ls:LSButton x:Name="NYS_ConfirmButton" Style="{DynamicResource SmallBrownButtonStyle}" Height="44" Padding="24,0" Margin="0,8,0,0"
                                                             HorizontalAlignment="Center" FontSize="{DynamicResource ScaledSmallishFontSize}"
                                                             Content="Confirm" Command="{Binding NysConfirmCommand}"
                                                             Visibility="{Binding NysShowConfirm, Converter={StaticResource BoolToVisibleConverter}, FallbackValue=Collapsed}"/>
                                                <ls:LSButton x:Name="NYS_SkipButton" Style="{DynamicResource SmallBrownButtonStyle}" Height="44" Padding="24,0" Margin="0,8,0,0"
                                                             HorizontalAlignment="Center" FontSize="{DynamicResource ScaledSmallishFontSize}"
                                                             Content="Skip" Command="{Binding NysSkipCommand}"
                                                             Visibility="{Binding NysShowSkip, Converter={StaticResource BoolToVisibleConverter}, FallbackValue=Collapsed}"/>
                                            </StackPanel>
                                            <TextBlock Text="{Binding Title}" Style="{StaticResource DetailedListText}" HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" Visibility="{Binding Title, Converter={StaticResource NullToCollapsedConverter}, ConverterParameter='EmptyString'}"/>
                                        </StackPanel>
                                    </Grid>

                                    <!-- ======== NAME Stack For Party ======== -->
                                    <Grid x:Name="NamePanelParty" Grid.Row="0" Visibility="{Binding IsPlayableCharacter, Converter={StaticResource BoolToVisibleConverter}}" DataContext="{Binding}">
                                        <StackPanel HorizontalAlignment="Center">
                                            <TextBlock Text="{Binding Stats.Race.DisplayName}" Style="{StaticResource SummaryListText}" TextWrapping="Wrap" TextAlignment="Center">
                                                <TextBlock.ToolTip>
                                                    <ls:LSTooltip IsHitTestVisible="False" Content="{Binding DataContext.Stats.Race.Description, ElementName=DetailedView}" ls:AttachedProperties.InheritedTag="{Binding DataContext.Stats.Race.DisplayName, ElementName=DetailedView}" ContentTemplate="{StaticResource HeaderBodyLocaStringTemplate}"/>
                                                </TextBlock.ToolTip>
                                            </TextBlock>
                                            <Image DataContext="{Binding Stats.ClassList[0]}" Style="{DynamicResource MainClassIconStyle}" Stretch="UniformToFill" Width="200" Margin="0,-6,0,-6">
                                                <Image.Effect>
                                                    <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="1" Color="{StaticResource tint00}"/>
                                                </Image.Effect>
                                            </Image>
                                            <TextBlock x:Name="SubclassLabel" HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" Text="{Binding Stats.ClassList[0].SubclassDisplayName}" Style="{StaticResource SummaryListText}" Visibility="{Binding Text, RelativeSource={RelativeSource Self}, Converter={StaticResource NullToCollapsedConverter}, ConverterParameter='EmptyString'}">
                                                <TextBlock.ToolTip>
                                                    <ls:LSTooltip IsHitTestVisible="False" Content="{Binding Path=DataContext.Stats.ClassList[0].SubclassDescription, ElementName=SubclassLabel}" ls:AttachedProperties.InheritedTag="{Binding DataContext.Stats.ClassList[0].SubclassDisplayName, ElementName=SubclassLabel}" ContentTemplate="{StaticResource HeaderBodyLocaStringTemplate}"/>

                                                </TextBlock.ToolTip>
                                            </TextBlock>

                                            <Control Template="{StaticResource PlayerMultiClass}" DataContext="{Binding}" />
                                        </StackPanel>
                                    </Grid>

                                    <!-- ======== ABILITIES INFO ======== -->
                                    <ItemsControl x:Name="Abilities" ItemsSource="{Binding Stats.Abilities}" VerticalAlignment="Center" Style="{StaticResource AbilitiesGridStyle}"/>

                                    <!-- ======== CONDITIONS ======== -->
                                    <StackPanel Visibility="{Binding StatusEffects.Count, Converter={StaticResource CountToVisibilityConverter}}">
                                        <ContentControl Style="{StaticResource BaseTitleStyle}">
                                            <TextBlock Text="{Binding Source='h3baf1fe2g7d16g4d77gbc35gc17a90fb27d6', Converter={StaticResource TranslatedStringConverter}}"/>
                                        </ContentControl>
                                        <Border Style="{StaticResource ListSectionBackground}" Visibility="{Binding StatusEffects.Count, Converter={StaticResource CountToVisibilityConverter}}">
                                            <ItemsControl x:Name="StatusHolder" ItemsSource="{Binding StatusEffects}" ItemTemplate="{StaticResource StatusListItemTemplate}" Background="Transparent" Margin="12"/>
                                        </Border>
                                    </StackPanel>

                                    <!-- ======== RESISTANCES ======== -->
                                    <StackPanel Name="ResistancesSection">
                                        <Grid>
                                            <ContentControl Style="{StaticResource BaseTitleStyle}">
                                                <TextBlock Text="{Binding Source='h98a4ccfbg20f4g4294g9bf7g624c95711539', Converter={StaticResource TranslatedStringConverter}}"/>
                                            </ContentControl>
                                        </Grid>
                                        <Control x:Name="ResistanceContainer" HorizontalAlignment="Center" Template="{StaticResource ResistancesGridTemplate}"/>
                                    </StackPanel>

                                    <!-- ======== FEATURES ======== -->

                                    <ContentControl Style="{StaticResource BaseTitleStyle}">
                                        <TextBlock Text="{Binding Source='h9f9bfc7dgf4cdg4198g83e3gcb974dd2e949', Converter={StaticResource TranslatedStringConverter}}"/>
                                    </ContentControl>

                                </StackPanel>
                                <Grid Name="FeaturesList" Grid.Row="1">
                                    <Border Style="{StaticResource ListSectionBackground}">
                                        <Grid VerticalAlignment="stretch">
                                            <b:Interaction.Behaviors>
                                                <ls:CollectionFilterBehavior x:Name="AllCharacterFeaturesPredicate" ItemsSource="{Binding Stats.Passives}" Predicate="{Binding DataContext.Data.AllCharacterFeaturesPredicate, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}"/>
                                                <ls:CollectionSortBehavior x:Name="AllCharacterFeaturesSorted" ItemsSource="{Binding FilteredItems, ElementName=AllCharacterFeaturesPredicate}" Comparer="{Binding DataContext.Data.PassivesSortComparer, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}"/>
                                                <ls:CollectionSortBehavior x:Name="FullListSorted" ItemsSource="{Binding Stats.Passives}" Comparer="{Binding DataContext.Data.PassivesSortComparer, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}"/>
                                            </b:Interaction.Behaviors>
                                            <ItemsControl x:Name="FeaturesHolder" ItemsSource="{Binding SortedItems, ElementName=AllCharacterFeaturesSorted}" ItemTemplate="{StaticResource FeaturesListItemTemplate}" Margin="12" Visibility="Visible"/>
                                        </Grid>
                                    </Border>
                                </Grid>

                            </Grid>
                        </ScrollViewer>
                        
                        <!-- ======== APPROVAL BAR ======== -->

                        <Grid Grid.Row="1" x:Name="ApprovalHolder" Margin="0,8,0,0" Visibility="{Binding PlayerCharacterProperties.ApprovalRatings.Count, Converter={StaticResource CountToVisibilityConverter}, ConverterParameter=0, FallbackValue={x:Static Visibility.Collapsed}}">
                            <StackPanel Orientation="Vertical">
                                <TextBlock Style="{StaticResource DetailedListText}" HorizontalAlignment="Center" Text="{Binding Source='hab8d8423g3797g4f2ega601g18f13af5add6',Converter={StaticResource TranslatedStringConverter}}"/>
                                <Grid HorizontalAlignment="Center">
                                    <Image VerticalAlignment="Center" Source="pack://application:,,,/Core;component/Assets/Shared/bar_mainFooter.png" Stretch="None"/>
                                    <Rectangle Width="448" Height="14" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,16">
                                        <Rectangle.Fill>
                                            <LinearGradientBrush>
                                                <GradientStop Color="{StaticResource AP_Negative}" Offset="0"/>
                                                <GradientStop Color="{StaticResource AP_Neutral}" Offset="0.33"/>
                                                <GradientStop Color="{StaticResource AP_Positive}" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Rectangle.Fill>
                                    </Rectangle>
                                    <Image HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,16" Source="pack://application:,,,/Core;component/Assets/Shared/bar_mainFooter_overlay.png" Stretch="None"/>

                                    <ItemsControl Width="448" Margin="0,0,0,16" ItemsSource="{Binding PlayerCharacterProperties.ApprovalRatings}" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <Grid/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid x:Name="BarRoot">
                                                    <Grid.Tag>
                                                        <Binding Path="Rating" Converter="{StaticResource AddConverter}" ConverterParameter="50"/>
                                                    </Grid.Tag>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <Rectangle Grid.Column="0" Fill="Transparent" Width="{Binding Tag, ElementName=Icon, Converter={StaticResource MultiplierConverter}, ConverterParameter=388}" MaxWidth="388"/>

                                                    <Ellipse x:Name="Icon" Grid.Column="1" Stroke="{StaticResource LS_disabledPadTxtColor}" StrokeThickness="3" Fill="{Binding Character.Icon}" Width="60" Height="60" VerticalAlignment="Center" Margin="0,0,-30,0" ToolTipService.Placement="Top" ToolTip="{StaticResource ManagedTooltip}">
                                                        <Ellipse.Tag>
                                                            <Binding Path="Tag" ElementName="BarRoot" Converter="{StaticResource DivideConverter}" ConverterParameter="150"/>
                                                        </Ellipse.Tag>
                                                        <Ellipse.Effect>
                                                            <DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="1" Color="{StaticResource tint00}"/>
                                                        </Ellipse.Effect>
                                                    </Ellipse>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </Grid>
                            </StackPanel>
                        </Grid>
                    </Grid>
              
                </TabItem>

                <!-- ====== SKILLS TAB =======-->
                <TabItem x:Name="SkillsTab" Visibility="{Binding IsPlayableCharacter, Converter={StaticResource BoolToVisibleConverter}}">
                    <TabItem.Header>
                        <Border>
                            <Border.ToolTip>
                                <ls:LSTooltip Content="{Binding Source='h12abe296ga0fbg44feg9451gb10a0bf92acb',Converter={StaticResource TranslatedStringConverter}}"/>
                            </Border.ToolTip>
                            <Image Width="60" Height="60" Source="pack://application:,,,/Core;component/Assets/CharacterCreation/ico_skills.png" Stretch="Uniform"/>
                        </Border>
                    </TabItem.Header>
                    <Control x:Name="SkillsList" Template="{StaticResource SkillsTemplate}"/>
                </TabItem>

                <!-- ====== DETAILS TAB ======-->
                <TabItem x:Name="DetailsTab">
                    <TabItem.Header>
                        <Border>
                            <Border.ToolTip>
                                <ls:LSTooltip Content="{Binding Source='he8e3236cgbf7eg4975gb5d8g3f4e3c0663f5',Converter={StaticResource TranslatedStringConverter}}"/>
                            </Border.ToolTip>
                            <Image Width="60" Height="60" Source="pack://application:,,,/Core;component/Assets/BottomBar/ico_identity_d.png" Stretch="Uniform"/>
                        </Border>
                    </TabItem.Header>
                    <ContentControl x:Name="DetailedView" Content="{Binding .}" ContentTemplate="{StaticResource CharacterSheetDetailedTemplate}" />
                </TabItem>

            </TabControl>
        </Grid>
        
        <ControlTemplate.Triggers>

            <!--Use NPC view for Summons-->
            <DataTrigger Binding="{Binding CharacterType}" Value="Summon">
                <Setter TargetName="NamePanelNPC" Property="Visibility" Value="Visible"/>
                <Setter TargetName="NamePanelParty" Property="Visibility" Value="Collapsed"/>
            </DataTrigger>

            <!--Use NPC view for Followers-->
            <DataTrigger Binding="{Binding CharacterType}" Value="Follower">
                <Setter TargetName="NamePanelNPC" Property="Visibility" Value="Visible"/>
                <Setter TargetName="NamePanelParty" Property="Visibility" Value="Collapsed"/>
            </DataTrigger>

            <!--Use Charactersheet in Companions Panel-->
            <DataTrigger Binding="{Binding Name, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}" Value="CompanionsPanel">
                <Setter TargetName="NamePanelNPC" Property="Visibility" Value="Collapsed"/>
                <Setter TargetName="NamePanelParty" Property="Visibility" Value="Visible"/>
                <Setter TargetName="SkillsTab" Property="Visibility" Value="Visible"/>
            </DataTrigger>

            <!--Use unfiltered Features list for Examine Panel-->
            <DataTrigger Binding="{Binding Name, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}" Value="Examine" >
                <Setter TargetName="FeaturesHolder" Property="ItemsSource" Value="{Binding SortedItems, ElementName=FullListSorted}"/>
            </DataTrigger>

            <!--show subclass when not multiclassing-->
            <DataTrigger Binding="{Binding stats.ClassList.Count, Converter={StaticResource GreaterThanConverter}, ConverterParameter=1}" Value="True" >
                <Setter TargetName="SubclassLabel" Property="Visibility" Value="Collapsed"/>
            </DataTrigger>

        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!-- Name Your Summons: vanilla options-menu dropdown art for the multi-summon
         mode selector. All self-contained pack:// URIs into the game's Core assembly
         (the same assets the vanilla Options dropdowns use), so there is no dependency
         on any other mod. -->
    <BitmapImage x:Key="NYS_PillDefault"    UriSource="pack://application:,,,/Core;component/Assets/Options/dropdownBox_wide_d.png"/>
    <BitmapImage x:Key="NYS_PillHighlight"  UriSource="pack://application:,,,/Core;component/Assets/Options/dropdownBox_wide_h.png"/>
    <BitmapImage x:Key="NYS_RoundDefault"   UriSource="pack://application:,,,/Core;component/Assets/Buttons/btn_small_round_d.png"/>
    <BitmapImage x:Key="NYS_RoundHighlight" UriSource="pack://application:,,,/Core;component/Assets/Buttons/btn_small_round_h.png"/>
    <BitmapImage x:Key="NYS_ArrowClosed"    UriSource="pack://application:,,,/Core;component/Assets/Options/input_dropUpArrow_d.png"/>
    <BitmapImage x:Key="NYS_ArrowClosedHL"  UriSource="pack://application:,,,/Core;component/Assets/Options/input_dropUpArrow_h.png"/>
    <BitmapImage x:Key="NYS_ArrowOpen"      UriSource="pack://application:,,,/Core;component/Assets/Options/input_dropDownArrow_d.png"/>
    <ImageSource x:Key="NYS_FlyoutSelected">pack://application:,,,/Core;component/Assets/Options/input_dropdown_flyout_selected.png</ImageSource>

    <Style x:Key="NYS_FlyoutItemText" TargetType="TextBlock">
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="FontSize" Value="{StaticResource ScaledDefaultFontSize}"/>
        <Setter Property="Foreground" Value="{StaticResource LS_accent75TxtColor}"/>
    </Style>

    <ControlTemplate x:Key="NYS_FlyoutItemTemplate" TargetType="Button">
        <Grid Background="Transparent" MinHeight="60">
            <Rectangle x:Name="fibg" Visibility="Collapsed">
                <Rectangle.Fill>
                    <ImageBrush ImageSource="{StaticResource NYS_FlyoutSelected}" Stretch="UniformToFill"/>
                </Rectangle.Fill>
            </Rectangle>
            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Left" Margin="24,0"/>
            <b:Interaction.Triggers>
                <b:EventTrigger EventName="MouseEnter">
                    <ls:LSPlaySound Sound="UI_Shared_Hover"/>
                </b:EventTrigger>
            </b:Interaction.Triggers>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="fibg" Property="Visibility" Value="Visible"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    </ls:UIWidget.Resources>


    <!-- ===== EXAMINE PANEL ===== -->
    <ls:UIWidget.Template>
        <ControlTemplate>
            <Grid x:Name="bgFade" Margin="-1400,0,-1400,-1000">
                <Grid x:Name="Panel" HorizontalAlignment="Center" VerticalAlignment="Top" 
              DataContext="{Binding CurrentPlayer.UIData.ExamineTarget}"  RenderTransformOrigin="0.5,0.5" Margin="0,220,0,0">
                    <Grid.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform/>
                            <SkewTransform/>
                            <RotateTransform/>
                            <TranslateTransform/>
                        </TransformGroup>
                    </Grid.RenderTransform>
                    <b:Interaction.Behaviors>
                        <ls:LSMouseDragElementBehavior HitArea="{Binding ElementName=DragHit,Mode=OneWay}"/>
                    </b:Interaction.Behaviors>
                    <ls:LSNineSliceImage x:Name="BGpanel" Style="{StaticResource Generic9SliceBG}" Width="{StaticResource ExaminePanelWidth}" Height="{StaticResource CSContentHeight}">
                        <Grid>
                            <!--PAPERDOLL-->
                            <Grid HorizontalAlignment="Left" Margin="8,0,32,0" >
                                <ls:LSPaperdoll x:Name="ExaminePaperdoll" EntityRef="{Binding EntityHandle}" Width="{StaticResource EXDollWidth}" Height="{StaticResource CSDollHeight}" UseCombatPose="True" MouseRotate="True" MouseRotateMultiplier="10" RenderScale="{Binding DataContext.Data.RenderScale, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}"/>
                                <!--CONTROLS-->
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,60">
                                    <ls:LSButton  x:Name="rotateRightBtn" Template="{StaticResource RotateRightButton}">
                                        <b:Interaction.Triggers>
                                            <b:EventTrigger EventName="LSButtonPressed">
                                                <b:ChangePropertyAction PropertyName="RotationSpeed" Value="1.2" TargetName="ExaminePaperdoll" />
                                            </b:EventTrigger>
                                            <b:EventTrigger EventName="LSButtonReleased" >
                                                <b:ChangePropertyAction PropertyName="RotationSpeed" Value="0" TargetName="ExaminePaperdoll" />
                                            </b:EventTrigger>
                                        </b:Interaction.Triggers>
                                    </ls:LSButton>
                                    <ls:LSButton  x:Name="rotateLeftBtn" Template="{StaticResource RotateLeftButton}">
                                        <b:Interaction.Triggers>
                                            <b:EventTrigger EventName="LSButtonPressed" >
                                                <b:ChangePropertyAction PropertyName="RotationSpeed" Value="-1.2" TargetName="ExaminePaperdoll" />
                                            </b:EventTrigger>
                                            <b:EventTrigger EventName="LSButtonReleased" >
                                                <b:ChangePropertyAction PropertyName="RotationSpeed" Value="0" TargetName="ExaminePaperdoll" />
                                            </b:EventTrigger>
                                        </b:Interaction.Triggers>
                                    </ls:LSButton>
                                </StackPanel>
                            </Grid>
                            <!--FOR ITEMS-->
                            <ls:LSNineSliceImage x:Name="ItemInfoPanel" Style="{StaticResource WorldTooltip9Slice}" VerticalAlignment="Stretch" HorizontalAlignment="Right">
                                <StackPanel>
                                    <!--Hit Points-->
                                    <Control x:Name="HPBarContainer" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,-28,0,-10" Template="{StaticResource TargetHealthBarTemplate}"
                                             Visibility="{Binding Stats.Health.Value, Converter={StaticResource CountToVisibilityConverter}, ConverterParameter=-1}"/>

                                    <ScrollViewer Style="{StaticResource NoForcedScrollViewerStyle}" Padding="{StaticResource ScrollOffset}" VerticalAlignment="Stretch" ClipToBounds="False" Margin="12,0">
                                        <StackPanel>
                                            <StackPanel.Resources>
                                                <ControlTemplate x:Key="DetailedEntry">
                                                    <ControlTemplate.Resources>
                                                        <Style TargetType="DockPanel">
                                                            <Setter Property="Margin" Value="8,0"/>
                                                        </Style>
                                                    </ControlTemplate.Resources>
                                                    <Border x:Name="entry" Style="{StaticResource RowBG_d}" MinHeight="48">
                                                        <ContentPresenter/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="entry" Property="Background" Value="{StaticResource RowBG_h}"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </StackPanel.Resources>

                                            <ContentPresenter ls:TooltipExtender.Owner="{Binding DataContext.CurrentPlayer.SelectedCharacter, ElementName=Examine}" Content="{Binding .}" DataContext="{Binding .}" ContentTemplate="{StaticResource ItemsTooltip}" ls:TooltipExtender.IsOpen="True" ls:TooltipExtender.Content="{Binding .}" ls:TooltipExtender.PlayerId="{Binding Path=DataContext.CurrentPlayer.PlayerId, ElementName=Examine}" Margin="20"/>

                                            <!--Resistances-->
                                            <StackPanel Name="ResistancesSection" Visibility="{Binding Stats.NumActiveResistances, Converter={StaticResource CountToVisibilityConverter}}">
                                                <ContentControl Style="{StaticResource BaseTitleStyle}">
                                                    <TextBlock Text="{Binding Source='h98a4ccfbg20f4g4294g9bf7g624c95711539', Converter={StaticResource TranslatedStringConverter}}"/>
                                                </ContentControl>
                                                <Control x:Name="ResistanceContainer" HorizontalAlignment="Center" Template="{StaticResource ResistancesGridTemplate}"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </ScrollViewer>
                                </StackPanel>

                            </ls:LSNineSliceImage>

                    <!--DRAG AREA-->
                            <Grid x:Name="DragHit" VerticalAlignment="Top" HorizontalAlignment="Stretch" Background="Transparent" Height="140">
                                <b:Interaction.Triggers>
                                    <b:EventTrigger EventName="MouseEnter">
                                        <b:InvokeCommandAction Command="{Binding DataContext.SetCursorCommand, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}" CommandParameter="Resize"/>
                                    </b:EventTrigger>
                                    <b:EventTrigger EventName="MouseLeave">
                                        <b:InvokeCommandAction Command="{Binding DataContext.SetCursorCommand, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}" CommandParameter=""/>
                                    </b:EventTrigger>
                                </b:Interaction.Triggers>
                            </Grid>
                            <!--CLOSE BUTTON-->
                            <ls:LSButton x:Name="CloseExamine" Style="{DynamicResource SmallCloseButton}" SoundID="UI_Shared_Cancel" HorizontalAlignment="Right" VerticalAlignment="Top" BoundEvent="UICancel" Margin="0,0,0,0">
                              <ls:LSButton.Triggers>
                                  <EventTrigger RoutedEvent="Button.Click">
                                      <BeginStoryboard x:Name="CloseAnimation" >
                                          <Storyboard Children="{StaticResource ClosePanelAnim}"/>
                                      </BeginStoryboard>
                                  </EventTrigger>
                              </ls:LSButton.Triggers>
                            </ls:LSButton>
                            <!-- ===== INFO PANEL ===== -->
                            <!--FOR CHARACTERS-->
                            <Control Name="CharacterInfoPanel" Template="{StaticResource CharSheetTabController}" Width="{StaticResource CSColumnWidth}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,8,8,8"/>

                        </Grid>
                    </ls:LSNineSliceImage>
                </Grid>

                <!-- ===== Name Your Summons: native settings overlay =====
                     DataContext is an SE viewmodel set from Lua (NativeConfigUI) when
                     the panel opens; the gear toggles IsOpen. FallbackValue keeps it
                     collapsed before the DataContext is bound. -->
                <Grid x:Name="NYS_SettingsPanel" HorizontalAlignment="Center" VerticalAlignment="Top" Panel.ZIndex="100">
                    <ls:LSNineSliceImage Style="{StaticResource Generic9SliceBG}" Width="1500" Height="1820" Margin="0,150,0,0"
                                         Visibility="{Binding NysIsOpen, Converter={StaticResource BoolToVisibleConverter}, FallbackValue=Collapsed}">
                        <Grid Margin="80,60,80,60">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0">
                                <ContentControl Style="{StaticResource BaseTitleStyle}" HorizontalAlignment="Center">
                                    <TextBlock Text="Name Your Summons"/>
                                </ContentControl>
                                <ls:LSButton Style="{DynamicResource SmallCloseButton}" HorizontalAlignment="Right" VerticalAlignment="Top"
                                             SoundID="UI_Shared_Cancel" Command="{Binding NysCloseCommand}"/>
                            </Grid>

                            <ScrollViewer Grid.Row="1" Style="{StaticResource NoForcedScrollViewerStyle}" Padding="{StaticResource ScrollOffset}" Margin="0,20,0,20">
                                <StackPanel>
                                    <!-- ===== Prompt ===== -->
                                    <ContentControl Style="{StaticResource BaseTitleStyle}">
                                        <TextBlock Text="Prompt"/>
                                    </ContentControl>
                                    <StackPanel Orientation="Horizontal" Margin="0,6">
                                        <ls:LSToggleButton Style="{DynamicResource TickBox}" VerticalAlignment="Center" IsChecked="{Binding NysPromptOnSummon, Mode=TwoWay}"/>
                                        <TextBlock Style="{StaticResource DetailedListText}" VerticalAlignment="Center" Margin="12,0,0,0" Text="Ask me to name new summons"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,6" IsEnabled="{Binding NysPromptForNamedEnabled}">
                                        <ls:LSToggleButton Style="{DynamicResource TickBox}" VerticalAlignment="Center" IsChecked="{Binding NysPromptForNamed, Mode=TwoWay}"/>
                                        <TextBlock Style="{StaticResource DetailedListText}" VerticalAlignment="Center" Margin="12,0,0,0" Text="Also re-ask for summons I have already named"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,6">
                                        <ls:LSToggleButton Style="{DynamicResource TickBox}" VerticalAlignment="Center" IsChecked="{Binding NysPauseOnPrompt, Mode=TwoWay}"/>
                                        <TextBlock Style="{StaticResource DetailedListText}" VerticalAlignment="Center" Margin="12,0,0,0" Text="Pause the game (turn-based mode) while I name a summon"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,6">
                                        <ls:LSToggleButton Style="{DynamicResource TickBox}" VerticalAlignment="Center" IsChecked="{Binding NysAllowStorySummons, Mode=TwoWay}"/>
                                        <TextBlock Style="{StaticResource DetailedListText}" VerticalAlignment="Center" Margin="12,0,0,0" Text="Allow renaming story-bound summons (e.g. 'Us')"/>
                                    </StackPanel>

                                    <!-- ===== Summon types to name (collapsible) ===== -->
                                    <ls:LSButton Style="{DynamicResource SmallBrownButtonStyle}" HorizontalAlignment="Left" Margin="0,20,0,0"
                                                 Content="Summon types to name..." Command="{Binding NysToggleTypesCommand}"/>
                                    <StackPanel Visibility="{Binding NysTypesExpanded, Converter={StaticResource BoolToVisibleConverter}, FallbackValue=Collapsed}">
                                        <TextBlock Style="{StaticResource DetailedListText}" TextWrapping="Wrap" Margin="0,6,0,0"
                                                   Text="Which summons to prompt for, by the creature type read from its tags. Familiars count as 'Familiars' whatever their type, so the creature types below apply to non-familiar summons only."/>
                                        <StackPanel Orientation="Horizontal" Margin="0,6" IsEnabled="{Binding NysNameEverySummonEnabled}">
                                            <ls:LSToggleButton Style="{DynamicResource TickBox}" VerticalAlignment="Center" IsChecked="{Binding NysNameEverySummon, Mode=TwoWay}"/>
                                            <TextBlock Style="{StaticResource DetailedListText}" VerticalAlignment="Center" Margin="12,0,0,0" Text="Every summon (ignore the type filter below)"/>
                                        </StackPanel>
                                        <ItemsControl ItemsSource="{Binding NysTypeToggles}" Margin="0,4,0,0">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal" Margin="0,4" IsEnabled="{Binding NysEnabled}">
                                                        <ls:LSToggleButton Style="{DynamicResource TickBox}" VerticalAlignment="Center" IsChecked="{Binding NysChecked, Mode=TwoWay}"/>
                                                        <TextBlock Style="{StaticResource DetailedListText}" VerticalAlignment="Center" Margin="12,0,0,0" Text="{Binding NysLabel}"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>

                                    <!-- ===== Multi-summon mode (vanilla options-menu dropdown) ===== -->
                                    <TextBlock Style="{StaticResource DetailedListText}" TextWrapping="Wrap" Margin="0,16,0,0"
                                               Text="When one spell summons several creatures at once:"/>
                                    <Grid HorizontalAlignment="Left" Width="520" Height="80" Margin="0,8,0,0">
                                        <ls:LSToggleButton x:Name="NYS_ModeToggle" SoundID="UI_HUD_Options_ComboBox"
                                                           HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                                           IsChecked="{Binding NysModeOpen, Mode=TwoWay}">
                                            <ls:LSToggleButton.Template>
                                                <ControlTemplate TargetType="ToggleButton">
                                                    <Grid Background="Transparent" VerticalAlignment="Stretch">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <ls:LSNineSliceImage Grid.Column="0" Grid.ColumnSpan="2" x:Name="pill"
                                                                             ImageSource="{StaticResource NYS_PillDefault}" VerticalAlignment="Stretch" Slices="40" IsHitTestVisible="False"/>
                                                        <Image Grid.Column="1" x:Name="rnd" Source="{StaticResource NYS_RoundDefault}" Stretch="None"
                                                               HorizontalAlignment="Center" VerticalAlignment="Center" Margin="12,0" IsHitTestVisible="False"/>
                                                        <Image Grid.Column="1" x:Name="arw" Source="{StaticResource NYS_ArrowClosed}" Stretch="None"
                                                               HorizontalAlignment="Center" VerticalAlignment="Center" IsHitTestVisible="False"/>
                                                    </Grid>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="pill" Property="ImageSource" Value="{StaticResource NYS_PillHighlight}"/>
                                                            <Setter TargetName="arw" Property="Source" Value="{StaticResource NYS_ArrowClosedHL}"/>
                                                        </Trigger>
                                                        <Trigger Property="IsPressed" Value="True">
                                                            <Setter TargetName="rnd" Property="Source" Value="{StaticResource NYS_RoundHighlight}"/>
                                                        </Trigger>
                                                        <Trigger Property="IsChecked" Value="True">
                                                            <Setter TargetName="arw" Property="Source" Value="{StaticResource NYS_ArrowOpen}"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </ls:LSToggleButton.Template>
                                        </ls:LSToggleButton>

                                        <!-- The current value, drawn over the pill (clicks pass through to the toggle). -->
                                        <TextBlock IsHitTestVisible="False" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="36,0,0,0"
                                                   Foreground="{StaticResource LS_accent100TxtColor}" FontSize="{StaticResource ScaledDefaultFontSize}">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="Do not name them"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding NysModeValue}" Value="shared">
                                                            <Setter Property="Text" Value="Share one name"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding NysModeValue}" Value="unique">
                                                            <Setter Property="Text" Value="Name each individually"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>

                                        <!-- The flyout: one entry per value. -->
                                        <Popup Placement="Bottom" PlacementTarget="{Binding ElementName=NYS_ModeToggle}"
                                               IsOpen="{Binding IsChecked, ElementName=NYS_ModeToggle}">
                                            <ls:LSNineSliceImage Style="{StaticResource PopoutNineSliceStyle}" MinWidth="{Binding ActualWidth, ElementName=NYS_ModeToggle}">
                                                <StackPanel Orientation="Vertical">
                                                    <Button Template="{StaticResource NYS_FlyoutItemTemplate}" HorizontalContentAlignment="Stretch"
                                                            Command="{Binding NysSelectSkipCommand}">
                                                        <TextBlock Style="{StaticResource NYS_FlyoutItemText}" Text="Do not name them"/>
                                                    </Button>
                                                    <Button Template="{StaticResource NYS_FlyoutItemTemplate}" HorizontalContentAlignment="Stretch"
                                                            Command="{Binding NysSelectSharedCommand}">
                                                        <TextBlock Style="{StaticResource NYS_FlyoutItemText}" Text="Share one name"/>
                                                    </Button>
                                                    <Button Template="{StaticResource NYS_FlyoutItemTemplate}" HorizontalContentAlignment="Stretch"
                                                            Command="{Binding NysSelectUniqueCommand}">
                                                        <TextBlock Style="{StaticResource NYS_FlyoutItemText}" Text="Name each individually"/>
                                                    </Button>
                                                </StackPanel>
                                            </ls:LSNineSliceImage>
                                        </Popup>
                                    </Grid>

                                    <!-- ===== Saved names ===== -->
                                    <ContentControl Style="{StaticResource BaseTitleStyle}" Margin="0,20,0,0">
                                        <TextBlock Text="Saved names"/>
                                    </ContentControl>
                                    <TextBlock Style="{StaticResource DetailedListText}" Text="No saved names yet."
                                               Visibility="{Binding NysHasSavedNames, Converter={StaticResource BoolToCollapsedConverter}}"/>
                                    <ItemsControl ItemsSource="{Binding NysSavedNames}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="0,6">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                                        <TextBlock Style="{StaticResource DetailedListText}" Text="{Binding NysOwnerLabel}"/>
                                                        <TextBlock Style="{StaticResource DetailedListText}" Text="{Binding NysRowLabel}"/>
                                                    </StackPanel>
                                                    <ls:LSTextBox Grid.Column="1" Width="520" MaxLines="1" TextWrapping="NoWrap" VerticalAlignment="Center"
                                                                  OpenVirtualKeyboardOnFocus="False"
                                                                  IsEnabled="{Binding NysNameEnabled}"
                                                                  Text="{Binding NysNameText, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"/>
                                                    <ls:LSButton Grid.Column="2" Style="{DynamicResource SmallBrownButtonStyle}" Margin="16,0,0,0" VerticalAlignment="Center"
                                                                 Content="{Binding NysForgetLabel}" Command="{Binding NysForgetCommand}"/>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </ScrollViewer>

                            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
                                <ls:LSButton Style="{DynamicResource SmallBrownButtonStyle}" Margin="20,0" Content="Refresh" Command="{Binding NysRefreshCommand}"/>
                            </StackPanel>
                        </Grid>
                    </ls:LSNineSliceImage>
                </Grid>

                <!--===== TRIGGERS & ANIMATIONS =====-->
                <b:Interaction.Triggers>
                    <b:StoryboardCompletedTrigger Storyboard="{Binding ElementName=CloseAnimation,Path=Storyboard}">
                        <b:InvokeCommandAction Command="{Binding CustomEvent}" CommandParameter="CloseWidget"/>
                    </b:StoryboardCompletedTrigger>
                </b:Interaction.Triggers>
                <Grid.Triggers>
                    <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                        <BeginStoryboard Storyboard="{StaticResource OpenPanelAnim}" />
                    </EventTrigger>
                </Grid.Triggers>
            </Grid>

            <ControlTemplate.Triggers>
                <DataTrigger Binding="{Binding CurrentPlayer.UIData.ExamineTarget.Type}" Value="Item">
                    <Setter TargetName="CharacterInfoPanel" Property="Visibility" Value="Collapsed"/>
                    <Setter TargetName="BGpanel" Property="Width" Value="2000"/>
                    
                </DataTrigger>
                <DataTrigger Binding="{Binding CurrentPlayer.UIData.ExamineTarget.Type}" Value="Character">
                    <Setter TargetName="ItemInfoPanel" Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </ControlTemplate.Triggers>

        </ControlTemplate>
    </ls:UIWidget.Template>

    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <ls:LSPlaySound Sound="UI_HUD_Examine_Open"/>
        </b:EventTrigger>
    </b:Interaction.Triggers>

</ls:UIWidget>