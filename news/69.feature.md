The multi-summon behaviour setting in the native settings panel is now a single
dropdown (styled like the game's own Options menu) instead of three separate
checkboxes, so it is clearer that only one option applies at a time.
