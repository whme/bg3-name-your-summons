Removed the "Always skipped" and "Skipped this session" managers from the
settings panel, along with the underlying skip persistence (the `SkippedSummons`
mod variable and its net channels). The per-creature Skip button used while
naming a multi-summon group is unchanged; that skip was never persisted.
