Opening the Examine panel on a summon no longer causes a noticeable stall.
Resolving the examined creature walked the whole UI tree asking every view-model
for properties it usually lacks, which logged thousands of engine warnings and
took several hundred milliseconds each time; it now reads each view-model's
property bag directly, where those values actually live, so the lookup is cheap.
