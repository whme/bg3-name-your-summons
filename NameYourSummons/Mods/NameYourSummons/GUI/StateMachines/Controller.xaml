<ls:StateMachine xmlns:ls="clr-namespace:ls;assembly=Code"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ls:StateMachine.States>

        <!-- Override ONLY the Examine state so the controller layout loads our
             modified Examine_c.xaml (a bare filename resolves to this mod's own
             Pages). The base controller Examine STATE is named "Examine" (not
             "Examine_c"; that is only the widget filename). Every other state merges
             from MainUI unchanged.

             We deliberately do NOT copy the base state's IE.UICancel -> RemoveState
             (GH #6): a state-level cancel closes the whole panel unconditionally, so
             the settings overlay could not make Circle close only itself. Closing
             Examine on Circle is instead driven by the widget's footer UICancel button
             (CustomEvent -> CloseWidget, handled below), which we can gate off while
             the settings overlay is open. The other four events are copied verbatim. -->
        <ls:State Name="Examine" ModType="Override" DisableStatesBelow="True" HideStatesBelow="False" IsModal="True" Layout="Player" Owner="Player">
            <ls:State.Widgets>
                <ls:StateWidget Filename="Examine_c.xaml" Layer="PopupPanels"/>
            </ls:State.Widgets>
            <ls:State.Events>
                <ls:StateEvent Name="IE.ToggleInGameMenu">
                    <ls:StateEvent.Actions>
                        <ls:PushState Name="Paused"/>
                        <ls:RemoveState/>
                    </ls:StateEvent.Actions>
                </ls:StateEvent>
                <ls:StateEvent Name="CloseWidget">
                    <ls:StateEvent.Actions>
                        <ls:RemoveState/>
                    </ls:StateEvent.Actions>
                </ls:StateEvent>
                <ls:StateEvent Name="OpenPinnedTooltips">
                    <ls:StateEvent.Actions>
                        <ls:AddSubstate Name="PinnedTooltips"/>
                    </ls:StateEvent.Actions>
                </ls:StateEvent>
                <ls:StateEvent Name="OpenMessageBox">
                    <ls:StateEvent.Actions>
                        <ls:AddSubstate Name="MessageBox"/>
                    </ls:StateEvent.Actions>
                </ls:StateEvent>
            </ls:State.Events>
        </ls:State>

    </ls:StateMachine.States>
</ls:StateMachine>
