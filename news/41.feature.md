The settings gear on a summon's Examine screen now opens a native settings
window built from the game's own UI, in place of the old overlay. It has the
same controls - prompt options, the per-creature-type filter, the multi-summon
mode, and the saved-name, always-skipped and session-skipped managers.
