The naming prompt and the in-game settings menu can now be navigated with a
controller: our controls are injected into the controller Examine layout and
join its native focus navigation, and naming a summon uses the game's built-in
on-screen keyboard, so a controller player can name summons without switching to
a keyboard (the keyboard still works too).
