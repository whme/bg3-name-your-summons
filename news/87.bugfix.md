Renaming a summon with keyboard and mouse no longer pops up Steam's on-screen
keyboard when Steam Input is active (for example in Steam Big Picture mode). The
overlay keyboard now only appears when naming with a controller.
