Reapplying a summon's saved name is now skipped when the summon already carries
that name. Re-summoning a named creature or loading a save previously rewrote
and re-replicated its display name unconditionally; it is now a no-op when
nothing would change.
